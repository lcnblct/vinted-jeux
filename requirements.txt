# Vinted Jeux — Watchlist FR
vinted_scraper==4.0.0
requests==2.34.2
pyyaml==6.0.3
python-dotenv==1.2.3
# LLM vision filter (optionnel, fail-open si pas de clé)
# openrouter + qwen3.7-flash déjà via requests, pas de dep supplémentaire
