# Monitor Vinted - Next Station Paris
# Option A: vinted_scraper (gère le mieux anti-bot Cloudflare + sync)
vinted_scraper>=1.3.0
requests>=2.31.0
pyyaml>=6.0
python-dotenv>=1.0.0

# UI Web
fastapi>=0.110.0
uvicorn[standard]>=0.30.0
jinja2>=3.1.0

# Option B alternative (décommenter si tu préfères vntd) :
# vntd>=1.1.0

# Option C alternative (async) :
# vinted-api-wrapper>=0.3.9
# vinted-api-kit>=1.0.1
